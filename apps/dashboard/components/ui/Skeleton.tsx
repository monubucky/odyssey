import { View } from "react-native";

export default function Skeleton() {
  return (
    <View
      style={{
        height: 70,
        backgroundColor: "#1F2430",
        borderRadius: 12,
      }}
    />
  );
}